#include <iostream>
#include <algorithm>
#include <cstdio>
#include <cstring>

using namespace std;

typedef unsigned long long ULL;

const int N = 1e5 + 10, P = 131;

ULL hx[N], t_hx[N];
int n;
int t_len[N];
char str[N], t_str;

int main()
{
  ios::sync_with_stdio(0);
	cin.tie(0);
  cin >> str + 1 >> n;
  for(int i = 0; i < n; i ++)

  
  return 0;
}

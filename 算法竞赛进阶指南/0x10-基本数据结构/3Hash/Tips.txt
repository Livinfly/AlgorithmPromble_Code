T1 获得经验，哈希不一定就一定一一对应，也可以是大致分类，然后在一个链表里寻找，是否有对应的值
T2 字符串哈希进制值可取131、13331较好。可以采用多哈希（不同的mod值，注意保成正数！！！
T3 二分写错也是醉了 while(l < r)
T4 一大错误是把它子串记录下来了MLE，其实只要记下它的开始地方即可，之后求hight之间用哈希值即可。还有学到一个新东西，字符串比较是逐位比较的，是O(n)的，可以用hash二分来做，变为logn